// Placeholder. We can use this module for routes that should be available without
// valid auth token (for example, public stats). We could also think about moving
// the auth route (to get the token) into this file. Or we just delete this module
// if we don't find any use for it.